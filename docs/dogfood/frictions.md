# Frictions — the dogfood log

Recorded the moment they happen; triaged at week's end. Severity: **blocker**
(stopped the work) · **grind** (works, but fights you) · **paper cut**
(noticed, moved on).

| # | date | bucket | during | friction | severity | disposition |
|---|---|---|---|---|---|---|
| 1 | 2026-07-10 | bluewaves | intake | `.html` is not a supported gate format — three real files (site-page exports) held in inbox on day one; the LibreOffice route could carry them | grind | **fixed** — html/htm joined LIBREOFFICE_EXTS (758f981 + slug fix 21c6b2d); verified live: both held files converted, inbox cleared |
| 2 | 2026-07-10 | bluewaves | intake | the gate detects duplicates only against sources/, not within the inbox — two byte-identical html files landed and only operator eyes caught it | paper cut | **fixed** — gate reports `inbox_dups` per batch (758f981); the twin was deleted and signed (`noted`, 2026-07-10) |
| 3 | 2026-07-10 | bluewaves | intake | sparse-text slides (<100 chars/page) flag needs_vision even with a complete text layer — 26 of 33 deck pages read visually; correct per the coverage invariant, but heavy | paper cut | **accepted** — the human's call (2026-07-10): the price of lossless processing |
| 4 | 2026-07-10 | bluewaves | intake | containers: a 144MB `.athena` delivery (zip: manifest.json + 11 groomed notes + 52 assets) has no route — the gate refuses what is really a vehicle full of Fusion-shaped content | grind | **decided** — unpack-as-delivery (human, 2026-07-10): container route scaffolded; members are the originals; archive-after-intake for past engagements |
| 5 | 2026-07-10 | bluewaves | intake | gate times out on large batches — similarity shingles multi-MB binaries against every source (O(n×m) with full-file decoding); 128-file delivery ran >2min | grind | **fixed** — SIMILARITY_READ_CAP + binary skip + shingle cache (6af4ce9); live: same bucket 0.35s vs >2min |
| 6 | 2026-07-10 | bluewaves | intake | nested container unpacked to inbox ROOT instead of beside its parent — dest is always inbox/<stem>, losing the folder context | paper cut | **fixed** — dest beside parent (eec8a67) + traversal containment at the root (8851fd3, 3e83d89) |
| 7 | 2026-07-10 | bluewaves | intake+librarian | delivery-manifest batch intake (122 admits/links) and broken-link repair (67 rewrites) both ran as hand-written operator scripts — the patterns are proven but not yet Fusion capabilities | grind | **fixed** — `convert.py batch` + references/delivery.md (b00b292); librarian link-repair scan/apply + sweep protocol (bcabdde, 62dbfae). Known limitation: code-span links scanned like prose — placeholder basenames rely on having no real candidate |
| 8 | 2026-07-10 | all | reflection | the checker and the librarian scanner read links inside code fences/spans as real links — 3 of bluewaves' 7 W4s were template placeholders in fenced examples | paper cut | **fixed** — code-blind link scan in both twins (f27d89c, dd8a785) + N-backtick spans (3a9a447); live: bluewaves 7→4 W4, all four honest. Known limits recorded: 4-space indented blocks still scanned; fence-closer indentation looser than CommonMark |
| 9 | 2026-07-11 | all | design review | six common lifecycle scenarios probed (first bucket, addition, conversion, git sync, new machine, external project): the skills' own guard blocked the onboarding conversation, ledger merges were undefined, dangling hub entries skipped silently, the resource: pattern was invisible | grind | **fixed** — the bucket-scenarios batch (this plan): guard becomes a door, union-merge .gitattributes at birth + SPEC §6/§7, hub truth in hub/today/agenda/check --hub, band-site worked example, BUCKETS-EVERYWHERE field guide |
| 10 | 2026-07-11 | all | setup verification | the user's agent-registry review caught two shipped defects: Pi and Codex natively read ~/.agents/skills (our links double-loaded every skill), and fan_out/--remove destroyed the canonical skills whenever an agent's skills dir was symlinked AT the canonical — this machine's own ~/.claude/skills topology (v1.1.0–v1.2.0) | blocker | **fixed** — v1.2.1: same-inode guard in fan_out/remove_all/_sweep_legacy, Pi/Codex flipped to standard mode, legacy links swept attribution-checked, destruction topology pinned in unit + installer-e2e; final review killed a second alias exploit (topology B) before ship |
| 11 | 2026-07-12 | family | intake | `admit` refuses every scanned/camera photo dropped as `.tif`/`.tiff` — `IMAGE_EXTS` never carried the tiff extensions, so the gate's fidelity promise ("preserve losslessly") was blocking on a format gap, not a real fidelity concern; the family photo import held on day one | grind | **fixed same-day** — user ruling (2026-07-12, verbatim: "Convert all tiffs to PNGs (no need for large tiff files) and improve the system to do it systematically"): `admit` now converts single-frame tiff → PNG deterministically at admit time (pillow, PEP 723 dep), the PNG becomes the stored original (sha256 in MANIFEST), and a ledger-visible note carries the source tiff's original filename + sha256 so the transformation stays honest. Multi-frame tiffs are refused outright (conservative — a silent first-frame-wins would violate the fidelity contract) rather than silently dropping frames |
| 12 | 2026-07-12 | family | librarian | after a document moved within `library/` (reorganizing the family import), repointing its MANIFEST `library` cell to the new path had no primitive — `link` only appends a new cell value, nothing removes/repoints the stale one, and `MANIFEST.md` is a single-writer register (never hand-edited, SKILL.md "Never") — the only way to fix a stale cell today is the forbidden hand-edit | grind | **fixed** — `convert.py relink` (6f708ea, v1.4.0) repoints a MANIFEST library cell in place; see row 14 |
| 13 | 2026-07-12 | bluewaves, family-business | push | sealed `.athena` containers had been COMMITTED — bluewaves had an already-processed inbound delivery lingering in `inbox/` and in git history (133MB), family-business had a set of outbound export archives in `output/` (879MB + 335MB) whose content was already fully represented in the bucket — both pushes to GitHub were refused; nothing in `fusion check` had flagged either bucket on the way there | blocker | **fixed same-day** — user ruling (2026-07-12): processed containers' content lives in `sources/`+`library/`; the archives themselves are never kept in the bucket. `fusion check` gains W6 (any `.athena`/`.zip` tracked anywhere — worst in `inbox/`, a delivery vehicle that should have been unpacked and discarded; elsewhere, a sealed archive whose content belongs unpacked) and W7 (any tracked file over 95MB — GitHub's hard limit is 100MB, warn before it hurts; both stay warnings, not errors, so a bucket stays usable offline). `fusion new`'s `.gitignore` template now ships `inbox/*.athena` + `inbox/*.zip` so the worst case never reaches a commit. The two live buckets were cleaned separately (verification the containers' content was already represented + history purge) and both push clean; live `fusion check` across all five real buckets confirms zero W6/W7 |
| 14 | 2026-07-12 | family-business | librarian (rulings reconcile) | the missing move/repoint primitive (friction 12) was hit a SECOND time, independently: re-linking 5 `pv-ag` MANIFEST rows after a librarian move forced a one-off audited script (`relink_pv.py`) that deliberately bypassed the single writer, because `convert.py link()` only appends to a cell — two bypasses in two days means the primitive is missing, not exotic. Related but distinct: 7flows' adopted records still carry room-era `source_file`/`source_path` provenance — but those live INSIDE historical document bodies (per-section provenance blocks of old plans), so repointing them would rewrite history; accepted as-is, the verb covers the go-forward case | grind | **fixed** — `convert.py relink` shipped on the single writer (6f708ea): row/old-value/target-exists assertions, multi-value cells preserved; SKILL.md + restructure.md carry the protocol. The 7flows room-era provenance blocks stay as-is (history is history) |
| 15 | 2026-07-12 | all five | first push | connecting a bucket to its GitHub remote is an undocumented dance — every remote had been created WITH a scaffold README, so every first push was rejected and needed the same manual recovery (fetch, `merge --allow-unrelated-histories`, keep ours on README) five times in a row; nothing in the docs says "create the remote empty" or shows the recovery | paper cut ×5 | **doc fix this session** — BUCKETS-EVERYWHERE gains "A bucket meets its remote": create the remote empty (no README/.gitignore/license); the root-join recovery recipe for when it's too late; never force-push |
| 16 | 2026-07-12 | family-business | reconcile | merging two originals into one doc had no `source:` convention — the ruling named both files, the schema doesn't constrain the field's type, so the implementer invented a YAML list and flagged it; 2 live docs now use a pattern no other doc uses | paper cut | **standardized** — SPEC §4 (4de1f5b): `source:` is a string; a list only for a doc reconciled from multiple originals. The 2 live docs already conform |
| 17 | 2026-07-12 | family-business | archive verification | the athena deletion rule ("every note must be represented") had no category for ZERO-CONTENT members — 3 ANAH notes were auto-generated placeholders ("aucune pièce présente dans les sources") that were never admitted and represent nothing; the call was made by operator judgment, and nothing in the gate or `fusion check` would flag a vacuous document if one ever landed in a bucket | paper cut | **fixed** — checker W8 (3eea7a2, v1.4.0): a summary-only document with no `source:`/`resource:` pointer (nothing beneath the closing separator) warns, never errors; SPEC §11 item 8. The pointer exemption is deliberate — a converted scan's designed shape IS summary + pointer (72 such docs in family-business stay silent, verified live). Boundary held: boilerplate stubs WITH text remain operator judgment |
